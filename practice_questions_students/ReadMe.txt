Step 1: Run the setup script
In MySQL Workbench, execute all SQL statements in "3.0_cw_Emp_Dept_Addr.sql".

This script will:

Create a new database named "cw1_db"

Create three tables:
	Department
	Address
	Employee

Insert sample records into each table

Step 2: Work on the query exercises
	Open "3.1_cw_Emp_Dept_Addr.sql".

Use the database "cw1_db" created in Step 1.

For each question, write the corresponding SELECT statement in MySQL Workbench.

The questions are grouped into four sections:

	1.Simple SELECT statements

	2.ETL-style queries (using SQL functions)

	3.Aggregations (GROUP BY)

	4.JOINs (INNER, LEFT, etc.)

Step 3: Save your work
After completing all queries, save your MySQL script using the format:

	1_sql_practice_<studentName>.sql

Step 4: Submit for review
	Upload your ".sql" file to GitHub and share the link with me for review (or) email to me.
